{
  "type": "object",
  "required": ["watches"],
  "properties": {
    "watches": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "zookeeper", "znode",
          "kafka", "target_topic", "transactional_id", "enable_idempotence",
          "max_transaction_retries"
        ],
        "properties": {
          "zookeeper": { "type": "string" },
          "znode": { "type": "string" },
          "kafka": { "type": "string" },
          "target_topic": { "type": "string" },
          "transactional_id": { "oneOf": [{ "type": "string" }, { "type": "null" }] },
          "enable_idempotence": { "type": "boolean" },
          "max_transaction_retries": { "type": "integer" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
